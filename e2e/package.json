{
  "name": "planman-e2e",
  "private": true,
  "version": "0.0.0",
  "description": "End-to-end tests for planman",
  "scripts": {
    "pretest": "cd .. && go build -o e2e/bin/planman .",
    "test": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1"
  }
}
